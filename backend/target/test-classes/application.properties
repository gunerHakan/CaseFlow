# --- Test Veritaban? (H2 In-Memory) ---
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# --- Redis Ayarlar? (Test Ortam?) ---
# GitHub Actions'da "redis" servisi tan?mlad?k, host ad? "localhost" olabilir (servis container yap?s?na göre de?i?ir)
# Ama yerelde test yaparken Redis yoksa hata alabiliriz.
# Testlerde Redis'i "mock"lamak en iyisidir ama ?imdilik basit tutal?m.
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.cache.type=none 
# Testlerde cache mekanizmas?n? devre d??? b?rakmak mant?kl? olabilir, böylece Redis zorunlulu?u kalkar.
